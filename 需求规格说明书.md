# 智能学习桌面助手 — 需求规格说明书（v5.2）

> 版本：v5.2  ·  日期：2026/08/24（最新工程实现同步见 v5.3 修订记录）
> 本文档为整套需求的正式存档，涵盖系统架构、硬件 BOM、状态机、业务逻辑、通信协议与工程注意事项。

---

## 一、系统定义

面向学习/办公场景的桌面设备，核心能力：

- 坐姿健康提醒（低头检测）
- 环境自适应（风扇自动调速、台灯自动调光）
- 效率工具（番茄钟、闹钟、学习时长统计）
- Web 远程控制（MQTT over WebSocket）
- 业务休眠（无人自动关风扇/台灯）

### 双芯片架构

| 角色 | 芯片 | 职责 |
|---|---|---|
| 协处理器 | ESP32‑S3‑N16R8（ESP‑IDF） | OV2640 采集、上半身关键点推理（鼻/双肩/双髋，可加双眼/双耳）、有人判定、头‑肩下沉角计算、WiFi/MQTT（关闭 BLE） |
| 主控 | STM32G474（FreeRTOS） | 全部业务逻辑、状态机、外设驱动、OLED/串口屏、RTC、Flash 存储 |

---

## 二、硬件清单（BOM）

| 序号 | 模块 | 型号/方案 | 接口/说明 |
|---|---|---|---|
| 1 | 主控 | STM32G474 | FreeRTOS；IWDG + 任务看门狗 |
| 2 | 协处理器 | ESP32‑S3‑N16R8 | 图像/模型放 PSRAM；关闭 BLE |
| 3 | 摄像头 | OV2640 | 160×160 采集，接 ESP32 |
| 4 | 温湿度传感器 | DHT11 | 单总线 GPIO |
| 5 | 光照传感器 | BH1750 | I2C |
| 6 | 加速度计 | MPU6050 | I2C，三轴挪动检测（带死区阈值） |
| 7 | 风扇 | 直流电机（平替）+ TB6612 驱动 | TIM PWM 调速（STM32 仅输出 PWM 信号，方向/使能固定接线） |
| 8 | 台灯 | WS2812 灯带 | 仅显示蓝色（软件固定 G=0/R=0/B=亮度 0~255）；TIM PWM+DMA 发 800kHz NRZ 码调光；灯带 5V 独立供电 |
| 9 | 坐姿提醒 LED | 普通小 LED（带正负极引脚） | 独立 GPIO 闪烁 |
| 10 | 触摸屏 | 淘晶驰 TJC4827T143_011R_1_P20 | 串口屏 |
| 11 | OLED | 0.96" SSD1306 | I2C，单页显示全部信息 |
| 12 | 提醒 | 无源蜂鸣器 | TIM PWM 驱动（配驱动管） |
| 13 | 时钟 | STM32G474 内建 RTC | VBAT 独立电源备份 |
| 14 | 供电 | MB‑102（HW‑227） | 5V/3.3V 双输出面包板电源 |

---

## 三、硬件接线要点

- **STM32 I2C 总线**挂载：OLED（SSD1306）、BH1750、MPU6050 —— 三器件地址互不冲突，可共总线。
- **DHT11** 用独立 GPIO（单总线时序敏感，需 4.7k 上拉）。
- **直流电机**：经 TB6612 驱动（STM32 仅 PWM 调速信号，PA11/TIM4_CH1；方向/使能固定接线 STBY/AIN1=VCC、AIN2=GND），不可 GPIO 直驱。
- **无源蜂鸣器**：不可 GPIO 直驱，须经驱动管 + 续流/保护二极管，TIM PWM 变频发声（PA6/TIM3_CH1，2~4kHz）。
- **台灯（WS2812 灯带）**：仅单色蓝、亮度可调，软件固定发送 G=0、R=0、B=亮度值(0~255)；TIM1_CH2 PWM+DMA 发 800kHz NRZ 码；灯带 5V 独立供电并预留电流余量。
- **ESP32 ↔ STM32 UART**：115200，两片共地。
- **电压匹配**：MB‑102 输出 5V（供电机/蜂鸣器）与 3.3V（供逻辑）分开接线，注意 ESP32‑S3‑N16R8 与 OV2640 的 3.3V 供电电流余量。

---

## 四、系统状态机

```
学习模式 ◄──切换──► 休闲模式
    │无人超时            │无人超时
    ▼                    ▼
      无人自动休眠（业务休眠）
   ▲   ↑唤醒：检测到有人 / MQTT / 触摸屏
   └───┴
```

| 模式 | 坐姿提醒 | 风扇/台灯 | 学习计时 | 番茄钟/闹钟 | 无人超时 |
|---|---|---|---|---|---|
| 学习 | ✅ | ✅ 自动+可手动 | ✅ | ✅（可开关/手动） | ✅ |
| 休闲 | ❌ | ✅ 自动+可手动 | ❌ | ✅ | ✅ |
| 业务休眠 | ❌ | ❌ 关闭 | ❌ | ✅ 后台运行 | — |

- **休眠判定**：STM32 周期上报 AI 状态帧（0x01，默认 1s/帧）；STM32 连续 ≥3 个上报周期无有效关键点帧（has_human=0 或 keypoint_valid 不足）即判定无人。
- **业务休眠**：仅关停风扇/台灯/坐姿判定；**摄像头检测保持开启**（用于"有人唤醒"）；OLED 保留时钟；番茄钟/闹钟后台继续（**到点允许蜂鸣器响铃，其余时间关停**）；MQTT 在线；此阶段 ESP32 可降至 2~3s/帧上报但不得停止。
- **唤醒**：检测到有人 / MQTT 指令 / 触摸屏操作。

> ⚠️ **当前固件状态**：无人自动休眠迁移逻辑在 `svc_state.c` 中以 `#if 0` **临时禁用**（串口屏调试期，
> 便于手动控制风扇/灯），三模式仍可手动切换（0x01 命令）；恢复后由 `UNHUMAN_FRAME_TH(3)` 周期计数自动休眠、检测到有人唤醒。

---

## 五、坐姿检测与校准

### 检测链路
ESP32 采集 160×160 → 上半身关键点（鼻/双肩/双髋，可加双眼/双耳）→ 置信度过滤 → 有人判定 → 头‑肩下沉角（鼻尖相对双肩连线中点的下沉角，低头为正，0.1° 精度）→ UART 上报 → STM32 时序滤波 + 状态判断。

### 低头告警触发条件（全部满足）
1. 有人判定成立（上半身关键点有效 ≥4 且含双肩），当前非无人休眠模式；
2. 头部关键点有效未丢失（头部可见）；
3. 当前头‑肩下沉角 > `angle_base + 告警阈值`；
4. 上述条件连续保持 ≥1.5s。

**处理**：坐姿提醒 LED 闪烁 + 告警冷却时间（防高频骚扰）；头部关键点丢失（如转头/被遮挡）时不做坐姿判定，但"有人"仍由躯干点维持（不误判无人）。

### 告警阈值（多档）
可选档位，默认 15°（如 10/15/20/25°，可配置存 Flash）——角度语义为**头‑肩下沉角**。

### 坐姿校准（非阻塞状态机）
1. 用户通过触摸屏或 Web（MQTT）下发【坐姿校准】；
2. 提示保持端正坐姿 2~3 秒（后台非阻塞采集，UI 显示进度），期间需"有人判定成立且头部关键点可见"；
3. ESP32 连续采集多帧有效上半身关键点，取头‑肩下沉角平均值作为基准角 `angle_base`；
4. 基准角通过 UART 发送给 STM32，STM32 保存到 Flash。

### 基准角失效检测（MPU6050 方案，执行者：STM32）
MPU6050 挂 STM32 I2C2（见《引脚规划 v3.3》），本检测由 **STM32 传感器任务**执行（ESP32 侧不承担加速度计监控）。
- **采样**：MPU6050 100ms 高频采样（传感器循环已重构为 20ms 步进，DHT11 2s / BH1750 500ms 节流）；
- **基准**：上电 **2s 后**建立基准（MPU6050 未稳定时读数不可靠），静止时每 **10s** 慢速更新（补偿零偏/温漂）；
- **三轴姿态差分**：X/Y 轴死区 `MOTION_DEADBAND_XY=200`（线性域 LSB，水平方向更灵敏）、Z 轴死区 `MOTION_DEADBAND_Z=400`；
- **持续确认窗口**：偏差超阈值需持续 `MOTION_CONFIRM_MS=200ms`（0.2s，两次采样即触发），避免"稍微挪动就要求校准"；
- 判定发生显著挪动 → `alert_flag=1` + **LED 常亮** + 提示"需要重新校准"（提示通道：LED + OLED + 触摸屏 + MQTT 上报 Web）；
- **LED 只靠校准完成熄灭**（ESP32 回传基准角 → `UE_CALIB_DONE` → `BspLed_Off()`），**不自动熄灭**。

---

## 六、环境控制与手动优先

| 设备 | 自动模式 | 手动模式 |
|---|---|---|
| 风扇（直流电机） | 依据温湿度 PWM 调速 | 用户设定档位 |
| 台灯（WS2812 单色蓝） | 依据 BH1750 光照调节 B 通道亮度(0~255) | 用户设定亮度 |

- **手动优先 = 手动持久**：启用手动后完全交由用户控制，直到用户**手动切回自动**才恢复（无超时自动恢复）。
- 控制模式（自动/手动）切换：触摸屏 + Web（MQTT）均可。

### 工程实现（v5.2 起）

- **风扇自动温度映射**（8 档，0~100%）：≤20°C 停 → 每 2°C 升 1 档（22/24/26/28/30/32°C）→ >32°C 最高档。
- **风扇软启动**：从停止启动先给 **40% Kick 400ms**（克服静摩擦），之后每 1s 最多 **+15%** 爬升限幅；高档启动也先压低，避免启动大电流；降速/保持立即执行。
- **启动风扇从"关"开始**：`SvcEnv_Init` 置 `s_fan_level=0`，避免上电即恢复高速档、在弱电源下触发复位循环。
- **台灯自动档为开关式**：光照 <200lux 全亮（255），否则灭（无阶梯）。
- **台灯每 5s 强制刷新**：恢复 3.3V 驱动 5V 灯带信号劣化/电源抖动导致的 WS2812 花屏/全白。
- **页面模式**：AUTO 页需"有人 + 光照<200lux"才开灯、按温度开风扇；MANUAL 页各设备按自动/手动挡独立控制、不看摄像头。

---

## 七、计时与安排工具

| 项 | 规则 |
|---|---|
| 本次学习时长 | 仅学习模式累加；退出停止；重进重计 |
| 今日学习时长 | 学习模式累加；RTC 日期跨天清零 |
| 总学习时长 | 全部累计；重置时写 Flash 置 0 |
| 番茄钟 | **全程手动：不随模式自动暂停/循环，只能手动暂停/继续/重置**，可开关 |
| 闹钟 | 可设置时/分、重置、开关；全模式生效；到点无源蜂鸣器响铃 |

**存储策略**：高频时长数据 RAM 累加、退出学习模式快照 Flash；低频配置分扇区写入；总时长重置写 0。

### 工程实现（v5.2 / v5.3 起）

- **番茄钟开关真正生效**：`s_pomo_en` 门控开始（开关关闭时 `PomoStart` 无效）；**设置时长(>0)自动打开开关**、设 0 自动关闭；默认时长 **0（关闭）**。
- **到点蜂鸣自动停**：番茄钟到点响 **10s**（`POMO_RING_MS`）、闹钟到点响 **30s**（`ALARM_RING_MS`）后自动停，避免一直响；**响一次模式响完自动关闭使能**。
- **默认值**：番茄钟 0 分钟/关闭、闹钟 **0:00**/关闭、**重复类型默认响一次（ONCE）**。
- **闹钟"点开才生效"（待生效缓冲）**：时/分/重复/星期的设置**只存待生效缓冲**（`s_pend_*`），点「开」（`AlarmEnable(1)`）才应用到实际并开始生效；拒绝时不改变使能。
- **每周必须先设星期（严格模式）**：每次选「每周」都清空星期标志（`s_weekday_set`），必须点「设星期」（0x12）后才能点「开」，否则被拒绝（`talstatus` 保持 CLOSE）；重启后已保存的每周设置保留生效。
- **非法星期锁存 ERR**：设星期输入 0 或 >7 → `trep`/`trep2` 显示 **ERR 并保持**，直到选一次/每日或设正确星期；同时 `talert` 一次性提示 `WEEKDAY 1-7!`。
- **未设置闹钟显示 NULL**：未设置（0:00）/重置后 `talarm`/`talarm2`/`trep`/`trep2` 均显示 **NULL**，用户设置过才刷新显示。
- **关铃（0x13）**：只停本次闹铃，不改变使能/设置，下次照常响。
- **开机恢复**：从 Flash 配置恢复番茄钟时长/开关、闹钟时/分/重复/使能。
- **配置可靠性**：配置落 Flash 带 **CRC16‑CCITT + 字段合法性校验 + magic 三重校验**，任一不过 → 恢复默认并重写；修复 G4 **双 bank** 下 bank2 扇区擦除失败问题（配置/时长扇区位于 bank2 末页）。

---

## 八、OLED（SSD1306, I2C）显示

**单页常显全部**：实时时钟、温湿度、光照、番茄钟倒计时。（学习模式如需附加本次/今日时长，与串口屏显示分工互补。）

---

## 九、触摸屏（淘晶驰 TJC 串口屏）菜单

1. 业务模式切换（学习 / 休闲 / 休眠）
2. 控制模式切换（手动 / 自动）
3. 风扇开关 + 调速
4. 台灯开关 + 亮度调节
5. 番茄钟：设置 / 重置 / 开关
6. 闹钟：设置 / 重置 / 开关
7. 显示今日学习时长、总学习时长
8. 重置总学习时长
9. 坐姿校准 + 多档阈值选择

> 本屏为 **USART HMI 指令集版（不支持 LUA）**，按钮事件用 `printh/prints` 组帧：
> `[0x55][0x0D][cmd][p1..p3 各 4B 小端][占位 2B]`（**占位不校验**）。
> STM32 侧 `bsp_tjc.c` 按此解析按钮事件（命令 0x01~0x13，含 0x12 闹钟星期 / 0x13 关铃，见 TJC_Project/README.md 命令表），
> 并每秒向两个页面推送状态文本（`page_auto.*.txt="..."`）。

---

## 十、通信协议

### 10.1 UART（STM32 ↔ ESP32）

帧格式：

```
[0xAA][0x55][CMD][LEN_H][LEN_L][PAYLOAD...][CRC16_H][CRC16_L]
```

- 帧头：2 字节 `0xAA 0x55`；
- LEN：2 字节，**仅表示 PAYLOAD 字节数**；
- CHECKSUM：**CRC16‑CCITT**，覆盖范围为 `[LEN_H → PAYLOAD]`（自 LEN 起始、含 CMD 与 PAYLOAD 的全部字节；帧头不参与校验）；
- 波特率：115200。

**帧收发约定**
- ESP32 周期上报 0x01，**默认 1s/帧**（依上半身姿态模型帧率约 1~3 FPS 内取值），业务休眠期可降至 2~3s/帧但不得停止。
- 无人/断链判定均按上报周期计数：连续 ≥3 周期无有效关键点帧 → 判无人；连续 ≥10 周期未收到任何 0x01 帧 → 判 UART 断链（§11.1），两者相互独立。

**命令表**

| 方向 | CMD | Payload |
|---|---|---|
| ESP32→STM32 | 0x01 | AI 状态：has_human(u8, 有人[上半身关键点达标]), head_visible(u8, 头部关键点可见), pitch_angle(i16, 0.1°, 低头为正·头‑肩下沉角), keypoint_valid(u8, 上半身有效关键点计数) |
| ESP32→STM32 | 0x02 | MQTT 指令（结构化二进制，避开 0xAA/0x55） |
| STM32→ESP32 | 0x10 | 触发坐姿校准（无 payload） |
| STM32→ESP32 | 0x11 | 设置 MQTT 上报周期：u8（秒，默认 3s） |
| STM32→ESP32 | 0x12 | 设备状态回传：模式、风扇档、台灯亮度、三种时长、闹钟状态 |
| 双向 | ACK | 关键指令（校准、闹钟/番茄钟设置、模式切换）回执 |

### 10.2 MQTT / Web 通信

- **Broker**：局域网 EMQX，端口 1883（TCP）；**另启用 WebSocket 监听口**供 Web 端连接。
- **鉴权**：开启用户名 + 密码，配置 **ACL 主题权限**；**不做 TLS 双向证书认证**。
- **主题**：
  - 订阅 `device/desk/ctrl`（控制指令，QoS1）；
  - 发布 `device/desk/report`（状态上报，QoS0，周期 3s）。
- **健壮性**：断线自动重连、离线指令缓冲（定义上限条数/时间窗，避免重连后积压指令一拥而上）。
- **遥控端**：Web 页面，浏览器 **WebSocket 直连 EMQX**；Web 用户名经 ACL 限制仅可访问 `device/desk/#`。

---

## 十一、软件任务划分

### 11.1 STM32G474（FreeRTOS）

- UART 解析（CRC16 校验 / ACK / 粘包处理）
- 传感器任务（DHT11 2s 节流、BH1750 500ms 节流、MPU6050 100ms 采样与挪动/基准角失效检测：三轴差分死区 XY=200/Z=400 + 200ms 持续确认）
- 状态机任务（三模式迁移、无人判定）
- 坐姿判定任务（头‑肩下沉角时序滤波 + 告警冷却）
- 环境控制任务（自动/手动优先级）
- 计时任务（时长 / 番茄钟 / 闹钟，RTC 驱动）
- 界面任务（OLED SSD1306、淘晶驰串口屏）
- Flash 存储任务（高频 RAM + 低频快照，分扇区；配置带 CRC16‑CCITT + 字段校验，失败自动恢复默认）
- 可靠性：IWDG + 任务看门狗（默认任务仅喂狗，**已移除"看门狗超时蜂鸣器"逻辑**）；UART 断链检测（连续 ≥10 个上报周期未收到任何 0x01 帧 → 判定 ESP32 断链，本地降级运行；与"无人休眠"判定相互独立）

### 11.2 ESP32‑S3‑N16R8（ESP‑IDF）

- 图像采集（OV2640，buffer 放 PSRAM）
- AI 推理（上半身关键点：鼻/双肩/双髋，可加双眼/双耳）+ 置信度过滤 + 有人判定 + 头‑肩低头角计算（低头为正）
- AI 状态周期上报（0x01，默认 1s；业务休眠期可降至 2~3s 保活）
- UART 收发（帧编码 / CRC16 / ACK 解析）
- 校准会话（非阻塞多帧平均，基准角回传）
- WiFi 连接 → 断线重连
- MQTT 客户端（订阅 ctrl QoS1 / 发布 report QoS0 3s / 离线缓冲）

---

## 十二、基于选定硬件的工程注意事项

1. **供电裕量**：MB‑102 面包板电源 USB 供电电流有限，**ESP32‑S3‑N16R8 + OV2640 工作电流较大**（可达数百 mA，含 WiFi 射频突发）。建议：电机/蜂鸣器走 5V，逻辑走 3.3V，给 ESP32 加足够去耦电容；若 WiFi 上报时偶发重启，需升级电源或降压模块。
2. **电机经 TB6612 驱动**（STM32 仅 PWM 调速信号，方向/使能固定接线），不可 GPIO 直驱；**蜂鸣器（无源）**需驱动管 + 续流/保护二极管，用 TIM PWM 变频发声。PWM 频率：电机 10~30kHz、蜂鸣器 2~4kHz。
3. **MPU6050 与 OLED/BH1750 共用 I2C**：地址（SSD1306 0x3C、BH1750 0x23/0x5C、MPU6050 0x68）无冲突，但 I2C 总线上拉与线长需控制。
4. **DHT11 精度较低**（温 ±2℃，湿 ±5%RH），影响"依温湿度调风扇"平顺性，可加一阶滤波；如需更准后续可换 SHT30（仅需改动驱动层）。
5. **阻塞读取问题**：DHT11/串口屏/OLED 均较慢，FreeRTOS 中勿在时间敏感任务内阻塞读取；DHT11 单总线时序需关中断，注意与看门狗喂狗冲突。
6. **WebSocket 直连 EMQX**：需在 EMQX 单独开 WS 监听端口；Web 端需内置用户名/密码 token，并用 ACL 限制 Web 用户名仅可订阅/发布 `device/desk/#`。
7. **离线指令缓冲**：定义缓冲上限（条数/时间窗），避免重连后指令积压。
8. **设备挪动检测**务必做死区 + 持续窗口：本工程采用三轴姿态差分（X/Y=200、Z=400 LSB）+ **200ms 持续确认**，并让 **LED 常亮**直到校准完成，否则轻碰即触发校准提示、或提示后无法复位。
9. **上半身关键点模型算力/帧率**：上半身姿态模型较 5 点人脸模型重，ESP32‑S3 + OV2640 160×160 预计 **1~3 FPS**；无人判定（≥3 周期）与坐姿告警（1.5s 保持）需按此帧率核算；休眠期降频省电，供电/去耦提示同第 1 条。
10. **供电/地弹教训（实际发生）**：风扇 + WS2812 曾共弱电源 → USB 过流保护跳闸、OLED/屏变暗、WS2812 闪全白、ST‑Link SWD 失稳后软件复位 MCU（SFTRSTF，非 BOR）。主因=电源电流不足（5V 跌落），次因=共地地弹；应共地 + 星形接地，电源建议 ≥2A。
11. **WS2812 掉压闪全白机制**：灯带芯片掉电重启时 DIN 悬空漂高 → 锁存 0xFFFFFF（全 1）。根治：强电源 + DIN 下拉（10k）+ 固件周期强制重发（台灯每 5s）。
12. **3.3V 驱动 5V 灯带**：信号高电平裕量不足，偶发花屏/噪声，固件 5s 强制刷新可自愈。
13. **G4 双 bank Flash**：DBANK=1 时 bank2 扇区擦除须用 `FLASH_BANK_2 + 页号`，否则擦除静默失败（配置永不持久化）；配置/时长扇区位于 bank2 末页（0x0807F000/0x0807F800）。
14. **蜂鸣器为低电平触发（实际接线）**：`BeepOff` 输出恒定高电平（CCR=ARR），适配低电平触发蜂鸣器。

---

## 十三、修订记录

| 版本 | 日期 | 变更摘要 |
|---|---|---|
| v1 | — | 原始需求梳理（含首轮评审：帧头/CRC/LEN/ACK/Flash 磨损/休眠功耗等） |
| v2 | — | 合入 14 条"完善" + 5 条"明确" |
| v3 | — | 确认基准角失效方案、触摸屏功能、EMQX/QoS/ACL、Web 端、番茄钟手动化、多档阈值 |
| **v4** | 2026/08/18 | 定稿：确认 DHT11/BH1750/MPU6050/直流电机平替/LED/淘晶驰屏/SSD1306/无源蜂鸣器/MB‑102 供电/WebSocket 直连 |
| **v5** | 2026/08/19 | 台灯澄清：WS2812 灯带仅显示蓝色（固定 G=0/R=0/B=亮度 0~255）、亮度可调；同步 BOM/接线/环境控制描述（对应《引脚规划 v3.1》） |
| **v5.1** | 2026/08/19 | 合入评审修订与检测方案升级：①检测改为**上半身关键点（鼻/双肩/双髋）+ 有人判定 + 头‑肩下沉角**（取代 5 点人脸俯仰角），0x01 增 head_visible；②MPU6050 挪动/基准角失效检测归 **STM32**（方案 A，删 ESP32 监控）；③风扇驱动改 **TB6612**；④CRC 覆盖范围纳入 LEN；⑤0x01 帧周期化（默认 1s，休眠期降频保活）并细分"无人/断链（≥3 周期/≥10 周期）"判定；⑥删 ESP32"WebSocket 桥接"；⑦明确休眠期闹钟/番茄钟到点可响铃（对应《引脚规划 v3.3》） |
| **v5.2** | 2026/08/24 | 工程实现同步（含修复）：①**配置持久化**——CRC16‑CCITT + 字段校验 + 修复 G4 双 bank 擦除；②**风扇**——温度阈值下移 3°C / 软启动 40% Kick +15%/s / 启动从 0 档；③**台灯**——自动挡开关式（<200lux 全亮）、5s 强制刷新；④**蜂鸣器**——低电平触发适配（BeepOff 恒定高）、番茄钟到点 10s 自动停、闹钟 30s 自动停；⑤**番茄钟**——开关真正生效、设时长自动开、默认 0 关闭；⑥**默认值**——番茄钟 0、闹钟 0:00；⑦删除看门狗超时蜂鸣器逻辑 |
| **v5.3** | 2026/08/25 | 闹钟交互与 MPU6050 灵敏度同步：①**闹钟"点开才生效"**（待生效缓冲）；②**每周必须先设星期**（严格模式，选每周清标志）；③**非法星期 ERR 锁存** + `talert` 提示；④闹钟**默认响一次（ONCE）**；⑤**trep/trep2 未设置显示 NULL**，设置后刷新；⑥**关铃命令 0x13**（只停本次）；⑦**MPU6050 三轴姿态差分**（XY=200/Z=400）+ **200ms 持续确认** + 上电 2s 建基准/静止 10s 慢更新；⑧**LED 常亮到校准完成**（不再自动熄灭）；⑨无人自动休眠 `#if 0` 临时禁用（串口屏调试期） |

