# 智能学习桌面助手 — 引脚规划（v3.3）

> 版本：v3.3  ·  日期：2026/08/19
> 依据：《需求规格说明书 v5.1》 + 用户对硬件的修改（DHT11/TJC 串口屏/WS2812/TB6612/I2C2）
> 引脚复用能力均经官方数据手册 DS12288（STM32G474xB/xC/xE, Rev 6, Table 12）与 STM32duino PeripheralPins.c 交叉核实。

---

## 一、架构与连接总览

```
                    ┌────────────────────────────┐
    OLED SSD1306 ──┐ │                            │
   BH1750 ────────┼─► STM32G474RET6              │
   MPU6050 ───────┘ │  (FreeRTOS, 主控)          │
   DHT11 ──────────► │                            │
   串口屏 TJC ──────► UART5                       │
   WS2812 ────────► TIM1_CH2                      │
   TB6612 ────────► TIM4_CH1                      │
   蜂鸣器 ────────► TIM3_CH1                      │
    提醒LED ────────► GPIO                         │
                    └──────────┬─────────────────┘
                               │ USART3 (115200, 共地)
                               ▼
                    ┌────────────────────────────┐
                    │  ESP32-S3-N16R8 (协处理器)  │──► OV2640 (DVP)
                    │   WiFi/MQTT / AI 推理       │
                    └────────────────────────────┘
```

- **电压匹配**：逻辑走 3.3V（双芯片/传感器/OLED），电机/蜂鸣器/WS2812 灯带走 5V；GPIO 仅给控制信号。
- **ESP32 ↔ STM32**：USART3，115200，TX↔RX 交叉，两片共地。

---

## 二、STM32G474RET6 引脚规划（LQFP64）

| 功能模块 | 引脚 | 外设复用 | 方向/说明 |
|---|---|---|---|
| ESP32 通讯 | PC10 / PC11 | USART3_TX / USART3_RX | TX↔RX 交叉对接、两片共地、115200 |
| 淘晶驰串口屏 TJC4827 | PC12 / PD2 | UART5_TX / UART5_RX | 专有帧协议收发 |
| 台灯 WS2812 灯带 | PA9 | TIM1_CH2 | 蓝色恒亮、B 通道值(0~255)调亮度；PWM+DMA 发 800kHz NRZ 码；灯带 5V 供电 |
| 风扇 TB6612 | PA11 | TIM4_CH1 (AF10) | →PWMA 调速；AIN1/STBY=VCC、AIN2=GND，恒速正转 |
| I2C2：OLED+BH1750+MPU6050 | PC4(SCL) / PA8(SDA) | I2C2 | 三器件共总线，4.7k×2 上拉 |
| DHT11 温湿度 | PB15 | GPIO | 独立 GPIO，+4.7k 上拉，读时序关中断 |
| 无源蜂鸣器（闹钟响铃） | PA6 | TIM3_CH1 (AF1) | PWM 变频发声（音频段 2~4kHz），经驱动管走 5V |
| 坐姿提醒 LED | PB4 | GPIO（推挽） | 高电平点亮：坐姿告警闪烁；MPU6050 挪动需校准**常亮**，校准完成熄灭 |
| SWD 调试 | PA13 / PA14 | SWDIO / SWCLK | 保留烧录/调试 |
| RTC 备份电源 | VBAT | — | 纽扣电池（如 CR1220） |
| 复位/启动 | NRST / BOOT0 | — | NRST 10k 上拉+100nF，BOOT0 下拉 |
| （空闲备用） | PB0/PB1/PB2 等 | — | TB6612 已改固定接线，释放备用 |

### I2C2 总线器件地址（互不冲突）

| 器件 | 地址 |
|---|---|
| SSD1306（OLED） | 0x3C |
| BH1750 | 0x23（ADDR 接地） |
| MPU6050 | 0x68 |

- 总线采用 400kHz 模式，外部 4.7k 上拉到 3.3V，线长尽量 <10cm。

### 风扇 TB6612 接线（简化，不经 G474）

| TB6612 引脚 | 接法 | 说明 |
|---|---|---|
| PWMA | ← PA11（TIM4_CH1 PWM） | 唯一受 G474 控制的调速信号 |
| AIN1 / STBY | → VCC(3.3V) | 恒速正转，STBY 高电平使能 |
| AIN2 | → GND | 恒速正转 |
| VM / VCC | 5V / 3.3V | 电机电压 / 逻辑电压 |

### WS2812 台灯（简化：单色蓝 + 亮度）

- 数据线：PA9（TIM1_CH2），3.3V 信号；灯带 5V 独立供电，注意电流余量。
- 只显示蓝色：软件固定发送 `G=0, R=0, B=亮度值(0~255)`；亮度即 B 通道值，实现调光。

---

## 三、ESP32-S3-N16R8 引脚规划

| 功能模块 | 引脚 | 说明 |
|---|---|---|
| UART1 ↔ STM32 | GPIO17(TX) / GPIO18(RX) | 与 STM32 PC10/PC11 交叉对接，共地 |
| OV2640 XCLK | GPIO15 | 输出时钟 ~10~20MHz |
| OV2640 SIOC / SIOD | GPIO16 / GPIO4 | SCCB 配置 |
| OV2640 VSYNC / HREF / PCLK | GPIO5 / GPIO6 / GPIO7 | 帧/行同步、像素时钟 |
| OV2640 D0–D7 | GPIO8,9,10,11,12,13,14,48 | 8 位并行数据 |
| OV2640 PWDN / RESET | GND / GPIO21 | PWDN 低=工作；RESET 低有效，接 GPIO21（原 GPIO3 为 strapping 脚，弃用）；RESET 建议外接 10k 下拉，防上电误复位 |
| USB-JTAG 调试 | GPIO19 / GPIO20 | 内置调试，免占串口 |
| WiFi 天线 | 板上 PCB 天线 | N16R8 模块自带 |

> OV2640 的 DVP 占用较多连续 GPIO；上述集合已避开 strapping（GPIO0/3/45/46）与串口/调试脚，仍建议以实际原理图/FPC 复核。

---

## 四、关键接线要点

1. **两芯片 UART 交叉**（STM32 PC10→ESP32 GPIO18、PC11→ESP32 GPIO17）+ 共地。
2. **I2C2 4.7k 上拉**：PC4(SCL)/PA8(SDA) 挂 OLED+BH1750+MPU6050。
3. **DHT11** 建议焊在 3.3V 侧，+4.7k 上拉；读时序段关中断，注意与喂狗冲突。
4. **电机/蜂鸣器走 5V**：PWM 引脚仅作控制信号，经驱动；WS2812 灯带 5V 供电并预留电流。
5. **给 ESP32-S3 + OV2640 充足去耦**（100nF + 10µF），因 WiFi 射频突发电流大，必要时独立供电。

---

## 五、引脚复用验证依据（官方数据手册 DS12288, Table 12）

| 引脚 | Alternate functions（原文摘录） | 结论 |
|---|---|---|
| PC4 | `TIM1_ETR, I2C2_SCL, USART1_TX, QUADSPI1_BK2_IO3, EVENTOUT` + ADC2_IN5 | **I2C2_SCL** |
| PA8 | `MCO, I2C3_SCL, I2C2_SDA, I2S2_MCK, TIM1_CH1, USART1_CK, ...` | **I2C2_SDA** |
| PA9 | `I2C3_SMBA, I2C2_SCL, I2S3_MCK, TIM1_CH2, USART1_TX, ...` | **TIM1_CH2** |
| PA11 | `SPI2_MOSI, TIM1_CH1N, USART1_CTS, COMP1_OUT, FDCAN1_RX, TIM4_CH1, TIM1_CH4, ...` | **TIM4_CH1**（同时可 TIM1_CH4） |
| PA6 | `TIM16_CH1, TIM3_CH1, TIM8_BKIN, SPI1_MISO, TIM1_BKIN, COMP1_OUT, QUADSPI1_BK1_IO3, LPUART1_CTS, EVENTOUT` | **TIM3_CH1** |
| UART5 | PC12=TX, PD2=RX | 标准映射 |

> 补充佐证：STM32duino PeripheralPins.c 中 `{PC_4,I2C2,AF4}`、`{PA_8,I2C2,AF4}`、`{PA_11_ALT2,TIM4,AF10,CH1}`。

---

## 六、对《需求规格说明书》的硬件相关更新

1. **台灯**：由"单个 LED PWM 调光"改为 **WS2812 灯带**（PA9/TIM1_CH2），单色蓝、亮度可调。
2. **风扇**：由"三极管/MOS 直驱"改为 **TB6612 驱动**（PA11/TIM4_CH1），且方向/使能为固定接线（STBY/AIN1=VCC、AIN2=GND），仅 PWM 调速（已同步《需求规格说明书 v5.1》）。
3. **DHT11**：接 PB15（原文档未定或建议独立脚）。
4. **串口屏**：淘晶驰 TJC4827 接 UART5（PC12/PD2）。
5. **I2C 总线**：OLED+BH1750+MPU6050 确认挂 **I2C2（PC4/PA8）** 共总线（§三"可共总线"成立，地址互不冲突）。
6. **检测方案升级**：由"5 点人脸关键点 + 俯仰角"升级为 **上半身关键点（鼻/双肩/双髋，可加双眼/双耳）+ 有人判定 + 头‑肩下沉角**（已同步《需求规格说明书 v5.1》）；对引脚无影响（OV2640 仍接 ESP32、UART 与供电保持不变）。

### 软件任务划分修订建议
- §11.2「ESP32 加速度计监控（MPU6050 挪动检测）」与 §11.1「STM32 传感器任务（含 MPU6050 读取）」存在器件归属冲突。
- **本规划采用方案 A**：MPU6050 挂在 STM32 I2C2，挪动检测及基准角失效判定归 STM32 传感器任务；ESP32 侧删除该监控项（已同步《需求规格说明书 v5.1》，基准角失效结果经 UART 与上位机交互，提示通道 OLED/触摸屏/MQTT）。

---

## 七、修订记录

| 版本 | 日期 | 变更摘要 |
|---|---|---|
| v1 | 2026/08/18 | 初稿：MPU6050 挂 STM32 I2C1，其余按默认复用 |
| v2 | 2026/08/18 | 按用户指定整改：DHT11=PB15、TJC=UART5(PC12/PD2)、WS2812=PA9/TIM1_CH2、TB6612=PA11、I2C2=PC4/PA8；并据数据手册核实引脚 AF |
| **v3** | 2026/08/18 | TB6612 方向/使能改固定接线（STBY/AIN1=VCC、AIN2=GND），仅 PWMA=PA11；WS2812 定稿为单色蓝、亮度可调；释放 PB0/PB1/PB2 |
| **v3.1** | 2026/08/19 | 采纳无源蜂鸣器 PA6/TIM3_CH1（闹钟响铃，PWM 变频 2~4kHz，经驱动管走 5V）；同步更新架构图与验证依据 |
| **v3.2** | 2026/08/19 | 同步引用《需求规格说明书 v5》：台灯 WS2812 仅单色蓝、亮度可调（G=0/R=0/B=0~255） |
| **v3.3** | 2026/08/19 | OV2640 RESET 由 GPIO3 改 **GPIO21**（避开 strapping，建议外接 10k 下拉）；§六 补第 6 条"检测方案升级为上半身关键点 + 头‑肩下沉角"；§六 第 2 条与方案 A 均标注已同步《需求规格说明书 v5.1》 |
