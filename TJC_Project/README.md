# 淘晶驰 TJC 串口屏工程搭建指南（智能学习桌面助手 · 指令集版）

- **屏幕**：TJC4827T143_011R_1_P20（4.3 寸 480×272，电阻触摸）
- **连接**：UART5（STM32 PC12-TX → 屏 RX；PD2-RX ← 屏 TX）
- **配套固件**：`bsp_tjc.c` / `app_tasks_flow.c` / `svc_timer.c`（本仓库已实现）
- **工具**：淘晶驰官网（tjc1688.com）下载 **USART HMI**
- **重要**：本屏是 **USART HMI 指令集**版本（**不支持 LUA**），所有控制代码
  直接写在**按钮的"弹起事件"**里，用指令 `printh`（发常量字节）/ `prints`（发变量）/ `dp`（跳页）。

> ⚠️ `.HMI` 主文件是二进制专有格式，必须用 USART HMI 创建布局。
> 本文档给出**全部控件清单 + 每个按钮的指令代码**，照抄即可，无需全局脚本。

---

## 页面结构

```
┌──────────────── page_auto（自动监测）────────────────┐
│  标题                                              │
│  番茄钟 HH:MM:SS   闹钟 HH:MM  重复(ONCE/DAILY/WEEKLY)│
│  今日时长 HH:MM:SS   总时长 HH:MM:SS   告警          │
│  [底部切换]  [自动监测] [手动控制]                    │
└──────────────────────────────────────────────────────┘
┌──────────────── page_manual（手动控制）───────────────┐
│  番茄钟 HH:MM:SS（倒计时显示）                        │
│  番茄 [开始][暂停][重置]  [分钟输入]+[设时长]          │
│  风扇 [自动][关][低][中][高]                          │
│  台灯 [自动][关][弱][中][强]                          │
│  闹钟 [时][分]输入 [一次][每日][每周]  [星期]+[设星期] │
│  [学习][休闲][校准][清零]                            │
│  [底部切换]  [自动监测] [手动控制]                    │
└──────────────────────────────────────────────────────┘
```

> 页面切换用 `dp=0/1`（**不能用 `page` 指令**，它会忽略后面所有代码）。
> 固件每秒向两个页面推送状态（带页前缀），跳页后照样刷新。
> 休眠逻辑由固件自动处理（无人→休眠→风扇/灯关闭、摄像头持续监测）。

---

## 一、新建工程

1. **USART HMI** → `文件 → 新建工程`，选型号 **TJC4827T143_011R_1_P20**
2. 把默认页（第 0 页）改名 **page_auto**，再新建一页（第 1 页）**page_manual**
3. 工程字符编码选 **UTF-8**（字库也选 UTF-8；若中文字库只有 GBK，工程也选 GBK）
4. **先生成字库**（TJC 无独立"字号"，字体大小 = 字库字高）：菜单 `工具 → 字库`

   | 字库名 | 字高 | 覆盖字符 | 用途 |
   |---|---|---|---|
   | ASCII16 | 16 | 英文/数字 | 番茄钟/闹钟/时长/重复/告警/按钮 |
   | 中文16 | 16 | 汉字 | 标题、中文标签/按钮 |

   **全部控件统一 16 字高**（ASCII16 / 中文16）。

5. **控件事件**：每个按钮选中 → 右侧 **事件** → 双击 **弹起** → 把下表指令**逐行**粘贴进去
   （取消勾选"发送键值"，按下事件不填）

---

## 二、page_auto（自动监测页）控件

### 显示区（全部文本控件，固件每秒推送，无需事件）

| 名称 | 类型 | 位置(x,y,w,h) | 说明 |
|---|---|---|---|
| ttitle | 文本 t | 0, 0, 480, 34 | 标题 `智能学习桌面助手` 字库 中文16 白字 深蓝底 |
| lpomo | 文本 t | 8, 40, 70, 28 | `番茄钟` |
| **tpomo** | 文本 t | 78, 34, 220, 40 | HH:MM:SS，字库 ASCII16（固件写） |
| lalarm | 文本 t | 8, 78, 70, 28 | `闹钟` |
| **talarm** | 文本 t | 78, 74, 150, 32 | HH:MM，字库 ASCII16（固件写） |
| **trep** | 文本 t | 230, 80, 150, 24 | ONCE/DAILY/星期缩写(如 WED)，字库 ASCII16（固件写；每周模式显示星期缩写） |
| **talstatus** | 文本 t | 300, 108, 80, 24 | OPEN/CLOSE（闹钟开关状态），字库 ASCII16（固件写，默认 CLOSE） |
| ltoday | 文本 t | 8, 114, 70, 28 | `今日` |
| **ttoday** | 文本 t | 78, 108, 220, 34 | HH:MM:SS，字库 ASCII16（固件写） |
| ltotal | 文本 t | 8, 150, 70, 28 | `总计` |
| **ttotal** | 文本 t | 78, 144, 220, 34 | HH:MM:SS，字库 ASCII16（固件写） |
| lalert | 文本 t | 8, 186, 70, 24 | `告警` |
| **talert** | 文本 t | 78, 186, 394, 24 | 红字（固件写 POSTURE!/LINK DOWN 等） |

### 底部切换按钮（y=214..272）

| 名称 | 显示文本 | 位置 | 弹起事件（逐行） |
|---|---|---|---|
| bpauto | 自动监测 | 8, 218, 228, 46 | （当前页，可空） |
| bpman | 手动控制 | 244, 218, 228, 46 | **`printh` 必须在前**：`printh 55 0d 02 01 00 00 00 00 00 00 00 00 00 00 00 00 00 00` 换行 `bpauto.bco=0x8410` 换行 `bpman.bco=0x07e0` 换行 `dp=1` |
| bbeepnen2 | 关铃 | 382, 218, 90, 46 | `printh 55 0d 13 00 00 00 00 00 00 00 00 00 00 00 00 00 00`（关闭本次闹铃，位置可调） |

**page_auto 页面"前初始化事件"**（双击页面空白处 → 事件 → 前初始化）：
```
bpauto.bco=0x07e0
bpman.bco=0x8410
```

---

## 三、page_manual（手动控制页）控件

按钮统一：**宽 74 × 高 24**，列 x = 8 / 90 / 172 / 254 / 336。

### 标题 + 番茄钟显示（y=0..54）

| 名称 | 类型 | 位置(x,y,w,h) | 说明 |
|---|---|---|---|
| ttitle2 | 文本 t | 0, 0, 480, 24 | 标题 `手动控制` 字库 中文16 白字 深蓝底 |
| lpomo2 | 文本 t | 8, 28, 56, 24 | `番茄钟` |
| **tpomo2** | 文本 t | 68, 22, 200, 32 | HH:MM:SS，字库 ASCII16（固件每秒写） |

### 行2（y=56）番茄钟控制

| 名称 | 文本 | 位置 | 弹起事件 |
|---|---|---|---|
| bstart | 开始 | 8, 56 | **先设时长(读npm)再启动**（4行）：`printh 55 0d 0d` 换行 `prints npm.val,0` 换行 `printh 00 00 00 00 00 00 00 00 00 00` 换行 `printh 55 0d 06 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| bpause | 暂停 | 90, 56 | `printh 55 0d 07 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| breset | 重置 | 172, 56 | `printh 55 0d 08 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |

> **「开始」按钮已合并"设时长 + 启动"**：输入 `npm` 分钟数后直接点「开始」即可，
> 无需先点「设时长」。暂停后点「开始」=继续（不重置）。`npm` 为 **0** 表示**关闭番茄钟**
> （开始无效、不响铃）；大于 **1000** 的异常值也按 0 处理（关闭番茄钟）。

### 行3（y=84）风扇

| 名称 | 文本 | 弹起事件 |
|---|---|---|
| bfa | 自动 | `printh 55 0d 03 ff ff ff ff 00 00 00 00 00 00 00 00 00 00 00 00` |
| bfoff | 关 | `printh 55 0d 03 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| bflow | 低 | `printh 55 0d 03 02 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| bfmid | 中 | `printh 55 0d 03 04 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| bfhigh | 高 | `printh 55 0d 03 06 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |

### 行4（y=112）台灯

| 名称 | 文本 | 弹起事件 |
|---|---|---|
| bla | 自动 | `printh 55 0d 04 ff ff ff ff 00 00 00 00 00 00 00 00 00 00 00 00` |
| bloff | 关 | `printh 55 0d 04 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| blweak | 弱 | `printh 55 0d 04 3c 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| blmid | 中 | `printh 55 0d 04 8c 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| blstrong | 强 | `printh 55 0d 04 e6 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |

### 行5（y=140）番茄钟时长设置（**分钟输入**，0~1440）

| 名称 | 类型 | 位置 | 说明 |
|---|---|---|---|
| lpmin | 文本 t | 8, 142, 56, 24 | `分钟` |
| npm | 数值 n（输入） | 64, 140, 60, 24 | 0~1000（0=关闭） |
| bpset | 按钮 | 132, 140, 204, 24 | `设时长`，弹起（3行）：`printh 55 0d 0d` 换行 `prints npm.val,0` 换行 `printh 00 00 00 00 00 00 00 00 00 00` |

### 行6（y=168）闹钟时间 + 重复类型

| 名称 | 类型 | 位置 | 说明 |
|---|---|---|---|
| lah | 文本 t | 8, 170, 24, 24 | `时` |
| nah | 数值 n（输入） | 32, 168, 50, 24 | 时 0~23 |
| lam | 文本 t | 88, 170, 24, 24 | `分` |
| nam | 数值 n（输入） | 112, 168, 50, 24 | 分 0~59 |
| balonce | 按钮 | 172, 168, 74, 24 | `一次`，弹起（4行）：`printh 55 0d 0c` 换行 `prints nah.val,0` 换行 `prints nam.val,0` 换行 `printh 00 00 00 00 00 00` |
| baldaily | 按钮 | 254, 168, 74, 24 | `每日`，同左但末行 `printh 01 00 00 00 00 00` |
| balweekly | 按钮 | 336, 168, 74, 24 | `每周`，同左但末行 `printh 02 00 00 00 00 00` |
| **talstatus2** | 文本 t | 414, 168, 60, 24 | OPEN/CLOSE（闹钟开关状态），字库 ASCII16（固件写，默认 CLOSE，位置可调） |
| **trep2** | 文本 t | 340, 196, 90, 24 | ONCE/DAILY/星期缩写(如 WED)，字库 ASCII16（固件写，位置可调） |
| **talarm2** | 文本 t | 340, 142, 90, 24 | HH:MM 或 NULL（手动页闹钟时间），字库 ASCII16（固件写，位置可调） |

> **闹钟时间显示**：两页的 `talarm`/`talarm2` 在**未设置或重置后显示 `NULL`**（0:00 视为未设置），
> 设置过非 0:00 时间后显示 `HH:MM`（待生效设置值）。
> **重复模式显示**：`trep`/`trep2` 在**未设置闹钟时显示 `NULL`**，用户设置过闹钟（点一次/每日/每周）后才刷新为 ONCE/DAILY/星期缩写。

### 行7（y=196）闹钟星期 + 开关

| 名称 | 类型 | 位置 | 说明 |
|---|---|---|---|
| lwd | 文本 t | 8, 198, 56, 24 | `星期(1~7)` |
| naw | 数值 n（输入） | 64, 196, 50, 24 | 1=周一..7=周日 |
| balwd | 按钮 | 124, 196, 74, 24 | `设星期`，弹起（3行）：`printh 55 0d 12` 换行 `prints naw.val,0` 换行 `printh 00 00 00 00 00 00 00 00 00 00` |
| balen | 按钮 | 206, 196, 74, 24 | `开`，弹起 `printh 55 0d 09 01 00 00 00 00 00 00 00 00 00 00 00 00 00 00`（**点开才应用上面设置的闹钟并生效**） |
| balrst | 按钮 | 288, 196, 74, 24 | `重置`，弹起 `printh 55 0d 0a 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| balnen | 按钮 | 378, 196, 74, 24 | `关`，弹起 `printh 55 0d 09 00 00 00 00 00 00 00 00 00 00 00 00 00 00`（关闭闹钟） |

> **闹钟逻辑（v5.2 起）**：时/分/重复/星期设置**只存待生效缓冲**，点「开」才应用到实际并开始生效；
> `talarm`/`trep` 实时显示**待生效设置**（设置后立即反馈），`talstatus` 显示开关状态 OPEN/CLOSE。
> **每周必须先设星期（严格模式）**：每次选「每周」（0x0C p3=2）都会**清空星期标志**，
> 必须点「设星期」（0x12）后才能点「开」，否则被拒绝（`talstatus` 保持 CLOSE）；
> 即"选每周 → 设星期 → 开"的完整顺序不能少。重启后已保存的每周设置保留生效（不强制重设）。
> **非法星期提示（锁存）**：`设星期`输入 0 或 >7 时，`trep`/`trep2` 显示 **`ERR` 并保持**（不自动恢复），
> 直到 **① 选「一次/每日」** 或 **② 设置正确的星期 1~7** 才恢复正常显示；
> 同时 `talert` 一次性提示 `WEEKDAY 1-7!`。
> **关闭本次闹铃**：手动/自动页的「关铃」按钮（0x13 指令）只停本次响铃，不改变使能，下次照常响。

### 手动页「关闭闹铃」按钮（bbeepnen）

| 名称 | 显示文本 | 位置 | 弹起事件 |
|---|---|---|---|
| bbeepnen | 关铃 | 8, 224, 74, 24 | `printh 55 0d 13 00 00 00 00 00 00 00 00 00 00 00 00 00 00`（关闭本次闹铃，位置可调） |

### 行8（y=224）模式/校准/其他

| 名称 | 文本 | 弹起事件 |
|---|---|---|
| bstudy | 学习 | `printh 55 0d 01 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| bleisure | 休闲 | `printh 55 0d 01 01 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| bcalib | 校准 | `printh 55 0d 05 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |
| btrst | 清零 | `printh 55 0d 0b 00 00 00 00 00 00 00 00 00 00 00 00 00 00` |

### 底部切换按钮（y=248..272）

| 名称 | 显示文本 | 位置 | 弹起事件（逐行） |
|---|---|---|---|
| bpauto2 | 自动监测 | 8, 248, 228, 24 | **`printh` 必须在前**：`printh 55 0d 02 00 00 00 00 00 00 00 00 00 00 00 00 00 00` 换行 `bpauto2.bco=0x8410` 换行 `bpman2.bco=0x07e0` 换行 `dp=0` |
| bpman2 | 手动控制 | 244, 248, 228, 24 | （当前页，可空） |

**page_manual 页面"前初始化事件"**：
```
bpauto2.bco=0x8410
bpman2.bco=0x07e0
```

---

## 四、事件配置要点

1. **弹起事件**里填上表指令，**逐行粘贴**（每行一条指令，换行分隔）
2. 取消勾选 **"发送键值"**；**按下事件**不填
3. **数字输入**：所有输入控件（npm、nah/nam、naw）勾选 **"输入"** 属性，
   点击后弹**数字键盘**直接输入（无需下拉框/状态开关）
4. **字体**：文本控件"字体"选对应字库（数字用 ASCII16，中文用 中文16）；选错则中文显示"？"
5. 固件写入的控件**名称必须与表格一致**（`tpomo`、`ttoday` 等，无下划线）
6. 页面切换用 **`dp=0`/`dp=1`** 赋值（**不要用 `page` 指令**——它会忽略其后所有代码）

---

## 五、互斥按钮"选中高亮"（可选增强）

TJC 按钮有背景色属性 **`bco`**（普通色）和 `pco`（按下瞬间色）。要实现"点谁谁高亮、其他按钮恢复默认色"，在**每个按钮的弹起事件**里同时设置本组全部按钮的 `bco`：

- 选中色：`0x07e0`（绿）
- 未选中色：`0x8410`（灰）

> 底部切换按钮 `bpauto/bpman` 已用此法（见前文），风扇/台灯/重复/模式按钮同理。

**示例：风扇挡位（bfa/bfoff/bflow/bfmid/bfhigh）**

`bflow`（低）弹起事件：
```
printh 55 0d 03 02 00 00 00 00 00 00 00 00 00 00 00 00 00 00
bfa.bco=0x8410
bfoff.bco=0x8410
bflow.bco=0x07e0
bfmid.bco=0x8410
bfhigh.bco=0x8410
```

其余挡位同理：被点中的按钮 `bco=0x07e0`，本组其他按钮 `bco=0x8410`。

**页面"前初始化"设默认选中**（如风扇默认"关"高亮）：
```
bfoff.bco=0x07e0
bfa.bco=0x8410
bflow.bco=0x8410
bfmid.bco=0x8410
bfhigh.bco=0x8410
```

同样做法应用于：
- **台灯**（bla/bloff/blweak/blmid/blstrong）
- **闹钟重复类型**（balonce/baldaily/balweekly）
- **模式**（bstudy/bleisure）
- 页面切换（bpauto/bpman、bpauto2/bpman2，已实现）
7. **切页按钮事件里 `printh`（发 0x02）必须放在 `dp` 之前**：`dp` 跳页会打断后续指令，
   放前面才能第一次按下就切换固件模式（否则要按两次）

---

## 五、编译与下载

1. `文件 → 编译`，无报错
2. USB 转 TTL：TX→屏 RX、RX→屏 TX、GND→GND、5V→屏 VCC（建议屏单独供电）
3. **先断开 STM32 的 UART5 两根线**再下载
4. `文件 → 下载`，选对 COM 口
5. 接回 STM32 UART5（PC12→屏 RX、PD2←屏 TX），复位上电

---

## 六、协议速查（与固件严格一致）

### 屏幕 → STM32（按钮事件帧，指令集组帧）

```
[0x55][0x0D][cmd][p1(4,LE)][p2(4,LE)][p3(4,LE)][占位2字节]
```
- 帧头/常量用 `printh`，变量用 `prints 控件.val,0`（4 字节小端）
- 末尾 2 字节为**占位**（`00 00`），屏幕不计算、STM32 **不校验**（指令集无法便捷算 CRC16-CCITT）

**帧格式示例**（固定命令一行）：`printh 55 0d <cmd> <p1 4B> <p2 4B> <p3 4B> 00 00`

### STM32 → 屏幕（字符串指令 + 3×0xFF）

```
page_auto.tpomo.txt="00:25:00"         (固件每秒推送, 文本赋值必须带 .txt)
page_manual.tpomo2.txt="00:25:00"      (手动页同步显示番茄钟)
page_auto.talarm.txt="07:30"
page_auto.trep.txt="DAILY"
page_auto.talert.txt="POSTURE!"
```

### 命令表（屏幕 → 固件）

| cmd | 功能 | 参数 |
|---|---|---|
| 0x01 | 系统模式 | p1: 0学习/1休闲/2休眠 |
| 0x02 | 外设控制 | p1: 0自动/1手动 |
| 0x03 | 风扇档位 | p1: 0关/1~7档/-1自动(0xFFFFFFFF) |
| 0x04 | 台灯亮度 | p1: 0~255/-1自动(0xFFFFFFFF) |
| 0x05 | 坐姿校准 | - |
| 0x06 | 番茄开始 | - |
| 0x07 | 番茄暂停 | - |
| 0x08 | 番茄重置 | - |
| 0x09 | 闹钟开关 | p1: 0/1 |
| 0x0A | 闹钟重置 | - |
| 0x0B | 总时长清零 | - |
| 0x0C | 闹钟设置 | p1:时 p2:分 **p3:0响一次/1每日/2每周几** |
| 0x0D | 番茄时长 | p1:分钟 |
| 0x0E | 阈值档 | p1: 0~3 |
| 0x0F | 番茄启用 | p1: 0/1 |
| 0x10 | 唤醒 | - |
| 0x11 | 校时 | p1:时 p2:分 p3:秒 |
| **0x12** | **闹钟星期** | **p1: 1=周一..7=周日** |
| **0x13** | **关铃** | -（只停本次闹铃，不改变使能/设置，下次照常响） |

---

## 七、模式与休眠逻辑（固件已实现，屏幕只需在切页时发控制模式）

**手动控制页**（切页时发 `0x02 p1=1`）：
1. 风扇/台灯各自有**自动挡/手动挡**：点「自动」→ 只看温度/光照自动调节（**不看摄像头**）；点具体档位/亮度 → 手动持久
2. 无人也不会自动关闭/休眠

**自动监测页**（切页时发 `0x02 p1=0`）：
1. **需摄像头判定"有人" + 温度/光照达到阈值** 才开风扇/台灯（有人且高温→风扇；有人且暗→台灯；无人则全关）

> 说明：`cmd_ctrl(0x02)` 只在**切页按钮**里发送（切到哪页就是哪种页面模式）。
> 手动页的「风扇自动/台灯自动」只切换**设备自动挡**，不影响页面模式。
> 完整休眠（SYS_MODE_SLEEP）仅由手动切换触发，不再因无人自动进入。

---

## 八、常见编译错误排查

| 报错 | 原因/解决 |
|---|---|
| `无效指令:function ...` 等 100+ 条 | 把 LUA 代码贴进来了 → 本屏**不支持 LUA**，删掉，按本文档用指令 |
| `无效指令:-- 注释` | 指令集**不支持 `--` 注释**，事件里只写指令 |
| `变量名称无效:xxx` | 控件名不合法（**不支持下划线**，只能用字母数字短名），按本文档命名 |
| `找不到标记:数字` | 控件名解析失败的连锁错误，改好控件名即可 |
| 中文显示"？" | 字库没选中文字库，或控件字体没选 中文16 |
| 控件能收到值但显示空白 | 文本控件的**"背景填充方式"**被误改成"切图" → 改回"单色"即恢复（曾导致番茄钟 tpomo 空白） |
| 字库超内存（9MB > 3.5MB） | 只保留 中文16 + ASCII16，删掉多余字库 |

