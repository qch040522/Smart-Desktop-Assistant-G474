# 台灯异常问题描述

## 问题概述

设备供电能力不足：电源无法同时支撑风扇高速运转与 10 颗 WS2812 灯带满载工作。在特定操作下，会引发一系列异常现象，最终导致台灯以错误的最高亮度显示。

## 问题链条

1. **电压跌落**：风扇转速过快 → 电流过大 → 超出供电能力 → 电源电压跌落
2. **MCU 复位**：电压跌落导致系统不稳定，触发 MCU 复位
3. **读取脏配置**：复位后 MCU 从 Flash 重新加载配置，其中台灯亮度字段已被污染成脏值（0xFF）
4. **默认最高亮度**：固件未对配置值做校验，脏值被当作合法亮度加载 → 台灯默认以最高亮度（255 全白）显示

## 伴随现象

- 电压跌落时，板载电源灯、OLED、传感器指示灯一起变暗
- 灯带数据信号受干扰，出现白闪、偶发错误颜色（如绿灯）
- 复位期间屏幕按键命令易丢失，操作无响应

---

## 解决方法

### 1. 脏值处理：配置加载时增加校验

加载 Flash 配置时做**三重校验**，任一不过即丢弃脏值、恢复默认值并重写干净配置：

1. **签名校验**：magic 字段必须匹配（0x4B4D31）
2. **CRC 校验**：整份配置计算 CRC16-CCITT，与存储值比对，检测任意字节被污染
3. **字段范围校验**：各字段必须在合法区间内

```
字段范围（超出即判脏值）：
  sys_mode < 3、ctrl_mode ≤ 1
  fan_level < 8、lamp_brightness ≤ 255
  alarm_repeat ≤ 2、alarm_weekday ≤ 7、pomodoro_min ≤ 1000
```

> 台灯亮度字段被污染成 `0xFF=255` 时，数值本身在合法范围（≤255），**仅靠字段校验无法识别**，必须依靠 **CRC 校验**才能发现"该字节本应是其他值却变成 0xFF"。

### 2. 校验逻辑完善：保存侧配合

- **保存时计算 CRC 并随配置写入**，下次加载可校验完整性
- **修复 Flash 擦除逻辑**（双 bank 配置下正确选择存储体与页号），确保配置能真正写入、脏值才能被覆盖替换

### 3. 辅助措施

- **启动时风扇从"关"开始**：避免恢复高速档在弱电源下反复复位
- **灯带周期性强制刷新**：可自愈偶发的错误锁存（如噪声导致的绿灯）

