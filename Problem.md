# 台灯异常问题描述

## 问题概述

设备供电能力不足：电源无法同时支撑风扇高速运转与 10 颗 WS2812 灯带满载工作。在特定操作下，会引发一系列异常现象，最终导致台灯以错误的最高亮度显示。

## 问题链条

1. **电压跌落**：风扇转速过快 → 电流过大 → 超出供电能力 → 电源电压跌落
2. **MCU 复位**：电压跌落导致系统不稳定，触发 MCU 复位
3. **读取脏配置**：复位后 MCU 从 Flash 重新加载配置，其中台灯亮度字段已被污染成脏值（0xFF）
4. **默认最高亮度**：固件未对配置值做校验，脏值被当作合法亮度加载 → 台灯默认以最高亮度（255 全白）显示

## 伴随现象

- 电压跌落时，板载电源灯、OLED、传感器指示灯一起变暗
- 灯带数据信号受干扰，出现白闪、偶发错误颜色（如绿灯）
- 复位期间屏幕按键命令易丢失，操作无响应

---

## 解决方法

### 1. 脏值处理：配置加载时增加校验

加载 Flash 配置时做**三重校验**，任一不过即丢弃脏值、恢复默认值并重写干净配置：

1. **签名校验**：magic 字段必须匹配（0x4B4D31）
2. **CRC 校验**：整份配置计算 CRC16-CCITT，与存储值比对，检测任意字节被污染
3. **字段范围校验**：各字段必须在合法区间内

```
字段范围（超出即判脏值）：
  sys_mode < 3、ctrl_mode ≤ 1
  fan_level < 8、lamp_brightness ≤ 255
  alarm_repeat ≤ 2、alarm_weekday ≤ 7、pomodoro_min ≤ 1000
```

> 台灯亮度字段被污染成 `0xFF=255` 时，数值本身在合法范围（≤255），**仅靠字段校验无法识别**，必须依靠 **CRC 校验**才能发现"该字节本应是其他值却变成 0xFF"。

### 2. 校验逻辑完善：保存侧配合

- **保存时计算 CRC 并随配置写入**，下次加载可校验完整性
- **修复 Flash 擦除逻辑**（双 bank 配置下正确选择存储体与页号），确保配置能真正写入、脏值才能被覆盖替换

### 3. 辅助措施

- **启动时风扇从"关"开始**：避免恢复高速档在弱电源下反复复位
- **灯带周期性强制刷新**：可自愈偶发的错误锁存（如噪声导致的绿灯）

---

# UI 卡顿 / 时钟跳秒问题

## 问题概述

触摸屏/OLED 上时钟显示偶尔跳秒：如秒从 `01` 直接跳到 `03`（跳过 `02`）。RTC 本身走时正常（经 RAM 采样验证秒严格 +1），问题出在 **UI 刷新任务被 TJC 串口发送阻塞**，导致下次读 RTC 时秒已跳过。

## 问题链条

1. **TJC 推送是阻塞发送**：`BspTjc_SetText` → `HAL_UART_Transmit(&huart5, ..., 200)`，每次调用等待数据发完（9600 波特率发 30 字节 ≈ 30ms，最坏等满 **200ms 超时**）；
2. **UI 每秒推 10 个文本控件**（番茄钟×2、今日/总时长、闹钟×2、重复×2、开关×2）：一次刷新最坏 `10 × 200ms = 2 秒`；
3. **UI 每 1000ms 刷新一次**：若单次刷新耗时 ≥1s，下一次判断 `(BSP_GetTick() - last_refresh) >= 1000` 立即满足，但实际间隔已 2s+ → 读到的 RTC 秒跳变（1→3 正好差 2s）；
4. OLED 也在同一刷新路径（Clear + Puts + Flush），叠加后进一步拖慢。

## 排查过程（排除项）

- **ESP32 0x04 时间同步不是原因**：`time_sync_trigger()` 无外部调用，仅 WiFi 连接后 `time_sync_start()` 发一次、之后每 3600s 校准——不会"经常"跳秒；
- **RTC 本身正常**：HOTPLUG 连续采样 `g_ui_dbg_time`，秒严格 +1 递增，无硬件走时问题；
- 定位结论：**UI 刷新路径的 TJC 阻塞发送**是根因（`TJC_TX_TIMEOUT_MS = 200u` 在 `bsp_tjc.c`）。

## 解决方法

### 1. TJC 推送去重（`app_tasks_flow.c`）

- 新增 `tjc_push()`：带缓存 + dirty 标志，**仅值变化时发送**（首次必发）；
- 静态控件（闹钟/重复/开关状态 ×6）不再每秒重复推送，每秒只发动态控件（番茄钟/今日/总时长 ×4）；
- 效果：单次刷新阻塞发送从最多 10 次降到 4 次。

### 2. TJC 发送改非阻塞（`bsp_tjc.c` / `bsp_uart_cb.c` / `bsp_tjc.h`）

- `tjc_send_bytes` 由阻塞 `HAL_UART_Transmit` 改为 **`HAL_UART_Transmit_IT` + 环形队列**（8 条 × 100 字节）：
  - 调用方（任务）**只入队立即返回**（微秒级），完全不被串口发送拖累；
  - UART5 TXE 中断逐条发送，`HAL_UART_TxCpltCallback`（`bsp_uart_cb.c`）自动续发下一条；
- 队列满时短暂 `osDelay(1)` 等待（正常每秒几条不会触发）；启动失败自动丢弃该条避免卡死；
- 未引入 DMA：UART5 未配置 DMA 通道，用中断方式改动最小。

## 验证

- 连续采样 `g_ui_dbg_time`：秒**严格连续 +1**（如 21:06:43 → 44 → … → 50），无跳变；
- 系统心跳/OLED/TJC 显示正常；
- 相关文件：`app_tasks_flow.c`、`bsp_tjc.c`、`bsp_tjc.h`、`bsp_uart_cb.c`。

## 经验教训

- **高频周期推送（尤其串口）务必去重 + 非阻塞**：每秒重复推送不变的文本既浪费带宽又可能阻塞调用方；
- 排查"显示跳秒/丢帧"时先分清是**数据源（RTC）**还是**消费路径（UI 刷新阻塞）**——本问题数据源正常，是消费路径被阻塞拖过了采样周期。

