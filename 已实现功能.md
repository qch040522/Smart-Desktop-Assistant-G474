# 智能学习桌面助手 — 已实现功能清单

> 对应《需求规格说明书.md》v5.2（2026/08/24）· 工程现状存档
> 主控：STM32G474（FreeRTOS）· 协处理器：ESP32‑S3（图像/AI/MQTT，与本仓库分仓维护）

---

## 一、系统框架（已实现）

- **FreeRTOS 多任务**：传感器 / 状态机 / 坐姿判定 / 环境控制 / 计时 / UI / Flash 存储 / 默认喂狗 等任务（见 `App/Src/app_tasks_*.c`）。
- **可靠性**：IWDG + 任务看门狗（各任务周期喂狗，默认任务仅喂狗、无额外动作）；UART 断链检测（≥10 个上报周期无 0x01 帧判断链，本地降级运行）。

## 二、业务状态机（已实现）

- 三模式：**学习 / 休闲 / 业务休眠**，支持无人超时自动休眠与"检测到有人 / MQTT / 触摸屏"唤醒。
- 休眠仅关停风扇/台灯/坐姿判定；RTC、OLED 时钟、番茄钟/闹钟后台继续（到点允许响铃）；上报周期降为 3s，唤醒恢复 1s。
- 有人判定基于 ESP32 上报的 `has_human` 连续 ≥3 周期判定；`occupy` 状态参与环境控制门控。

## 三、环境控制（已实现）

- **风扇**（TIM4_CH1 + TB6612，8 档 0~100%）：
  - 自动档按温度映射：≤20°C 停 → 每 2°C 一档 → >32°C 最高档；
  - 软启动：从停止先给 40% Kick 400ms，之后每 1s 最多 +15% 爬升；启动时风扇从"关"开始（防弱电源复位循环）。
- **台灯**（TIM1_CH2 + WS2812，单色蓝，亮度 0~255）：
  - 自动档开关式：光照 <200lux 全亮，否则灭；
  - 每 5s 强制刷新，自愈 3.3V 驱动 5V 灯带的信号劣化/花屏。
- **页面模式**：自动监测页（需有人+阈值才开）/ 手动控制页（设备按钮=隐式切手动，手动持久直到手动切回自动）。
- 手动切入休眠强制关停风扇/台灯。

## 四、坐姿检测与校准（已实现）

- 坐姿判定：基于 ESP32 上报的头‑肩下沉角（0.1°），连续保持 ≥1.5s 触发告警；LED 闪烁提醒 + 60s 冷却。
- 多档阈值：10/15/20/25°（默认第 2 档），阈值档位持久化 Flash。
- 坐姿校准：触摸屏/上位机触发，非阻塞采集 → 基准角平均值回写 Flash。
- MPU6050 挪动/基准角失效检测：三轴合成量偏差 + 死区 + 持续 2s 确认窗口 → 提示重新校准。

## 五、计时工具（已实现）

- **学习时长**：本次（仅学习模式）/ 今日（跨天清零）/ 总时长（重置写 Flash 0），退出学习模式快照落盘。
- **番茄钟**：设置/开始/暂停/重置/开关；开关真正生效（关闭时开始无效）；**设置时长(>0)自动打开开关**；到点蜂鸣 **10s** 自动停；默认 0 分钟（关闭）。
- **闹钟**：时/分/重复（一次/每日/每周几）/开关；全模式生效；到点响铃 **30s** 自动停；默认 0:00。
- **开机恢复**：从 Flash 恢复番茄钟时长/开关、闹钟时/分/重复/使能。

## 六、显示（已实现）

- **OLED（SSD1306, I2C）**：单页常显时钟、温湿度、光照、番茄钟倒计时。
- **淘晶驰 TJC 串口屏（UART5, 9600）**：USART HMI 帧协议（`55 0D` 帧 + CRC）；业务模式/控制模式切换、风扇/台灯控制、番茄钟/闹钟设置、时长显示、坐姿校准、阈值选择、RTC 校时。

## 七、配置持久化（已实现）

- 配置快照 Flash（0x0807F800，bank2 末页）；时长快照（0x0807F000）。
- **三重校验**：magic 签名 + CRC16‑CCITT + 字段合法性校验，任一不过 → 恢复默认并重写干净配置。
- 修复 G4 **双 bank** 擦除 bug（bank2 扇区用 `FLASH_BANK_2 + 页号`）。
- 配置项：业务/控制模式、坐姿基准角/阈值、闹钟、番茄钟、风扇档、台灯亮度等。

## 八、通信（已实现对接）

- **UART（STM32 ↔ ESP32, 115200）**：`AA 55 + CMD + LEN + PAYLOAD + CRC16` 帧；周期上报 0x01（AI 状态）、命令下发 0x02、0x10 校准触发、0x11 上报周期、0x12 设备状态回传、ACK。
- **TJC 串口屏协议**：`0x55 0x0D` 帧 + 16 位 CRC，命令/事件双向。
- MQTT / Web（ESP32 侧实现，STM32 仅暴露状态上报与命令接口）。

## 九、RTC 与校时（已实现）

- STM32G474 内建 RTC，VBAT 备份；支持触摸屏/上位机校时（时/分/秒），日期跨天处理今日时长清零。

## 十、外设驱动（已实现）

| 外设 | 接口 | 状态 |
|---|---|---|
| DHT11 温湿度 | PB15 单总线（关中断时序 + DWT 微秒判定） | ✅ |
| BH1750 光照 | I2C2 | ✅ |
| MPU6050 加速度 | I2C2（挪动/基准失效检测） | ✅ |
| WS2812 灯带 | TIM1_CH2 PWM‑DMA（800kHz NRZ） | ✅ |
| 无源蜂鸣器 | TIM3_CH1 PWM 变频（低电平触发，BeepOff 恒定高） | ✅ |
| OLED SSD1306 | I2C2 | ✅ |
| 风扇 + TB6612 | TIM4_CH1 | ✅ |
| 坐姿提醒 LED | GPIO 闪烁 | ✅ |
| 内部 Flash | 双 bank（配置/时长扇区） | ✅ |

## 十一、命令集（TJC/上位机 → STM32）

模式切换 / 唤醒 / 告警阈值档 / 页面控制模式 / 风扇档 / 台灯亮度 / 坐姿校准 / 番茄钟（开关·时长·开始·暂停·重置）/ 闹钟（设置·每周几·开关·重置）/ 重置总时长 / RTC 校时。

## 十二、关键参数速查

| 参数 | 值 |
|---|---|
| 风扇档数 | 8（0=关） |
| 风扇软启动 | Kick 40% / 400ms，+15%/s |
| 台灯亮度 | 0~255（自动：<200lux 全亮） |
| 番茄钟到点蜂鸣 | 10s 自动停 |
| 闹钟到点蜂鸣 | 30s 自动停 |
| 番茄钟/闹钟默认 | 0 分钟 / 0:00，均关闭 |
| 坐姿告警保持/冷却 | 1.5s / 60s |
| 配置/时长扇区 | 0x0807F800 / 0x0807F000（bank2 末页） |

## 十三、已知问题 / 注意事项

- 3.3V 驱动 5V WS2812 信号裕量不足，偶发花屏（5s 强制刷新自愈）；弱电源下风扇高速可能电压跌落导致 ST‑Link 复位 MCU（建议 ≥2A 供电、共地星形接地）。
- ESP32 协处理器（图像/AI/MQTT）不在本仓库，需另行维护；上位机侧命令接口见 §八。
